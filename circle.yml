machine:
  environment:
    NODE_ENV: development
    DISABLE_RAVEN: true

  node:
    version: 6.3.0

dependencies:
  cache_directories:
    - "client/src/vendor"

deployment:
  production:
    branch: master
    heroku:
      appname: mathew-kleppin

